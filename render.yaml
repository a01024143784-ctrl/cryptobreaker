services:
  - type: web
    name: cryptobreaker
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --worker-class gthread --threads 8 --timeout 0 --keep-alive 75 app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
